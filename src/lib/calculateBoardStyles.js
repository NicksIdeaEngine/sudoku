function calculateStyles(maxRows = 9, maxColumns = 9, regionSize = 3) {
  const tileWidth = 3.5;
  const tileBorderWidth = 0.2;
  const offset = 0.2;
  const boardContainerWidth = 35.1;

  // .sudoku-tile { width: 3.5em; height: 3.5em; }
  // .sudoku-container { max-width: 26.75em; }
}
